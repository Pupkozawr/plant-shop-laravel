@extends('layouts.app')
@section('content')
<div class="card" style="max-width:800px"><h1>Редактировать товар</h1><form method="POST" action="{{ route('admin.products.update', $product) }}">@csrf @method('PUT')<label>Название<input type="text" name="name" value="{{ old('name', $product->name) }}"></label><label>Описание<textarea name="description">{{ old('description', $product->description) }}</textarea></label><label>Цена<input type="number" step="0.01" name="price" value="{{ old('price', $product->price) }}"></label><label>Категория<select name="category_id">@foreach($categories as $category)<option value="{{ $category->id }}" @selected(old('category_id', $product->category_id)==$category->id)>{{ $category->name }}</option>@endforeach</select></label><label>Ссылка на изображение<input type="url" name="image_url" value="{{ old('image_url', $product->image_url) }}"></label><label>Остаток<input type="number" name="stock" value="{{ old('stock', $product->stock) }}"></label><label><input type="checkbox" name="is_active" value="1" @checked(old('is_active', $product->is_active))> Активный товар</label><button class="btn">Сохранить</button></form></div>
@endsection
