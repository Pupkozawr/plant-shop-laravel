@extends('layouts.app')
@section('content')
<h1>Каталог растений</h1><form class="card" method="GET"><div class="row"><div class="w-70"><label>Поиск<input type="text" name="search" value="{{ request('search') }}" placeholder="Например: монстера"></label></div><div class="w-30"><label>Категория<select name="category"><option value="">Все</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected(request('category')==$category->id)>{{ $category->name }}</option>@endforeach</select></label></div></div><button class="btn">Найти</button></form><div class="grid" style="margin-top:20px">@forelse($products as $product)<div class="card product-card"><div class="product-visual">{{ $product->category->name }}</div><h3 class="product-title">{{ $product->name }}</h3><div class="product-meta"><span class="badge">{{ $product->category->name }}</span><span class="price-chip">{{ $product->price }} ₽</span></div><div class="product-actions"><a class="btn" href="{{ route('catalog.show', $product) }}">Подробнее</a></div></div>@empty<p>Ничего не найдено.</p>@endforelse</div><div style="margin-top:20px">{{ $products->links() }}</div>
@endsection
